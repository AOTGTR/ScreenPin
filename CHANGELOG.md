# Changelog

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/) · เวอร์ชันตาม [SemVer](https://semver.org/)

## [Unreleased]

## [1.0.3] - 2026-08-21

### เพิ่ม
- **รองรับจอที่วางแนวตั้ง / เป็นตาราง** — หน้าหลักวาดการ์ดจอเป็นแถวตามผังจริง
  จอซ้อนบน-ล่างหรือ 2×2 ก็แสดงถูกต้อง
- ปุ่มลัด **`Ctrl+Alt+↑` / `Ctrl+Alt+↓`** ย้ายหน้าต่างขึ้น/ลงจอ
  (ในผังจอที่ไม่มีจอด้านนั้น จะไม่ทำอะไร ไม่ย้ายมั่ว)
- ปุ่ม ←↑↓→ ใน overlay เลือกจอ เดินตามทิศจริงของจอแล้ว
- **GitHub Actions** — ตรวจ syntax/import, build `ScreenPin.exe`, ลองเปิดจริง
  แล้วแนบเข้า release อัตโนมัติเมื่อ push tag
- README มีหัวข้อภาษาอังกฤษ

### แก้
- เทสไม่ hard-code ว่าต้องมี 3 จอแล้ว รันได้ตั้งแต่ 2 จอขึ้นไป

## [1.0.2] - 2026-08-21

### แก้
- **แอพที่ย่ออยู่ไม่หายจากหน้าจอแล้ว** — ขึ้นเป็นไอคอนสีเทา + ▾ บนจอที่มันสังกัด
  ลากย้ายได้ทั้งที่ยังย่อ (เดิมโดนกรองทิ้งเพราะหน้าต่างที่ย่อมี rect แค่ 160×28
  และ `MonitorFromWindow` คืน NULL ให้หน้าต่างที่จอดอยู่ที่ −32000)
- **แอพที่เปิดจาก Chrome/Edge shortcut (TikTok, YouTube) มีไอคอนแล้ว** —
  พวกนี้ส่งไอคอนเปล่ามา ตอนนี้ตรวจเจอแล้ว fallback ไปใช้ไอคอนของเบราว์เซอร์
- แปลงไอคอนเร็วขึ้นเกือบเท่าตัว (slice assignment แทน loop ต่อ pixel)
  และดึงไอคอนแบบ lazy ไม่อยู่ใน loop หลักอีกต่อไป

### เพิ่ม
- LICENSE (MIT)

## [1.0.1] - 2026-08-20

### แก้
- **ไอคอนบน taskbar เป็นของ ScreenPin แล้ว** — เดิมขึ้นเป็นไอคอน Edge เพราะ
  หน้าตาใช้ Edge เป็นตัว render (ตั้ง AppUserModelID + RelaunchIconResource
  ให้หน้าต่าง และให้ shortcut ใน Start menu ใช้ id เดียวกัน)
- **กดย่อหน้าต่างแล้วไม่เด้งกลับขึ้นมาเองแล้ว** — ตัวกันหน้าต่างหาย
  เข้าใจผิดว่า "ย่อ" = "ถูกซ่อน" เลยดึงกลับทุกวินาที

### เพิ่ม
- slot 5-6 สำหรับคนมีจอเยอะ (เว้นว่างไว้ ตั้งปุ่มเอง)
- ปุ่มลัดที่เว้นว่าง ไม่นับเป็น "ชนกับโปรแกรมอื่น" อีกแล้ว

## [1.0.0] - 2026-08-20

เวอร์ชันแรก

- ลากไอคอนแอพข้ามจอ เห็นไอคอนจริงของแต่ละหน้าต่าง
- จำจอของทุกหน้าต่าง 2 ชั้น (ต่อหน้าต่าง / ต่อแอพ) คืนอัตโนมัติเมื่อจอกลับมา
- ID จอถาวรจาก EDID ไม่ผูกกับเลข 1-2-3 ของ Windows
- ปักหมุดแอพไว้จอถาวร
- ปุ่มลัดทั่วเครื่อง + overlay เลือกจอ (GDI)
- อพยพทั้งจอก่อนปิดจอ โดยยังจำจอเดิม
- ตัวแอพเองมี chain จอหลัก/จอรอง
- tray icon, Start menu shortcut, เปิดพร้อม Windows
- แพ็คเป็น `ScreenPin.exe` ไฟล์เดียว

[Unreleased]: https://github.com/AOTGTR/ScreenPin/compare/v1.0.3...HEAD
[1.0.3]: https://github.com/AOTGTR/ScreenPin/releases/tag/v1.0.3
[1.0.2]: https://github.com/AOTGTR/ScreenPin/releases/tag/v1.0.2
[1.0.1]: https://github.com/AOTGTR/ScreenPin/releases/tag/v1.0.1
[1.0.0]: https://github.com/AOTGTR/ScreenPin/releases/tag/v1.0.0
